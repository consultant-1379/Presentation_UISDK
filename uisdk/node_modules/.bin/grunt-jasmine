#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-jasmine/bin/grunt-jasmine" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-jasmine/bin/grunt-jasmine" "$@"
  ret=$?
fi
exit $ret
