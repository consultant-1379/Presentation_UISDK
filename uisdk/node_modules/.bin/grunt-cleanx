#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-cleanx/bin/grunt-cleanx" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-cleanx/bin/grunt-cleanx" "$@"
  ret=$?
fi
exit $ret
